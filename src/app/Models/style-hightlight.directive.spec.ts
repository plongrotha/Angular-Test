import { StyleHightlightDirective } from './style-hightlight.directive';

describe('StyleHightlightDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleHightlightDirective();
    expect(directive).toBeTruthy();
  });
});
